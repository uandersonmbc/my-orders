import React from 'react';

import { Layout, Divider } from 'antd';

import { Redirect } from 'react-router-dom';

const styles = {
    background: '#5E274F', minHeight: '100vh'
}

export default function Login() {

    return (
        <Layout style={styles}>

        </Layout>
    );
}